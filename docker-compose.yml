services:
  mongo:
    image: mongo:7
    container_name: buildplay-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - ./data/mongo:/data/db
    command: ["--quiet"]
